name: Debug Drift
on:
  workflow_dispatch:
jobs:
  drift:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: "stable"
      - run: flutter --version
      - name: format drift
        run: |
          flutter pub get
          dart format .
          echo "----- git diff after dart format -----"
          git --no-pager diff
